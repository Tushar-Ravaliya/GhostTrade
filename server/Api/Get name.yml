info:
  name: Get name
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/v1/market/name
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
