info:
  name: Login
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/v1/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
