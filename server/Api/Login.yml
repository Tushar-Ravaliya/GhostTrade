info:
  name: Login
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/market/market
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
