info:
  name: Market Gainer
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/api/v1/market/gainer
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
