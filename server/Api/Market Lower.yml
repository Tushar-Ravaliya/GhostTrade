info:
  name: Market Lower
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/api/v1/market/lower
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
