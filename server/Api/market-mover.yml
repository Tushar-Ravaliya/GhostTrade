info:
  name: market-mover
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/api/v1/market/market-movers-free
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
