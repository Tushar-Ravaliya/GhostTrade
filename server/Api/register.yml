info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "name":"Ania",
        "email":"ania@gmail.com",
        "mobileNo":1234567890,
        "password":"Ania@03"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
