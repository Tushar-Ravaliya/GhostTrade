info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "name":"akira",
        "email":"ak@gmail.com",
        "mobileNo":1234567890,
        "password":"akira"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
