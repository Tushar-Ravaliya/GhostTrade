info:
  name: twelvefetch
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/v1/market/timeseries/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
